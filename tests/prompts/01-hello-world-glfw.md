I'm starting a fresh C++ project that will use Dear ImGui. I'm on Linux (Ubuntu 22.04 + clang 17), CMake is installed, and I'd like the docking branch.

Just get me to a basic GLFW + OpenGL3 hello-world that renders ImGui's demo window so I can browse what's available before I build anything real. CMakeLists should fetch ImGui and GLFW for me — I don't want to deal with system packages or submodules.

Put everything under `tests/01-hello-world-glfw/` and include a README with the build/run commands.
