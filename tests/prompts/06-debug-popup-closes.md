My Dear ImGui (docking branch) delete-confirmation modal flashes open for one frame and then immediately closes whenever the user clicks the trigger button. I see the modal's title bar appear and vanish on the same frame. Standard `BeginPopupModal` with OK/Cancel buttons inside.

Why the one-frame flash? Save your reply (markdown, with the corrected code) to `tests/06-debug-popup-closes/response.md`.
